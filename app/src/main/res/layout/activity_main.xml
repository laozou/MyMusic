<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".MainActivity">

    <ImageButton
        android:contentDescription="@string/music_Play_Pause"
        android:id="@+id/star_pause_music_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/button_grey_play"

        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

    <ImageButton
        android:contentDescription="@string/musicNext"
        android:id="@+id/next_music_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/button_grey_next"
        android:layout_alignParentBottom="true"
        android:layout_toRightOf="@+id/star_pause_music_button"
        android:layout_toEndOf="@+id/star_pause_music_button" />

    <ImageButton
        android:contentDescription="@string/musicPrev"
        android:id="@+id/prev_music_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/button_grey_prev"
        android:layout_alignParentBottom="true"
        android:layout_toLeftOf="@+id/star_pause_music_button"
        android:layout_toStartOf="@+id/star_pause_music_button" />

    <TextView
        android:id="@+id/the_music_Name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/musicName"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:layout_above="@+id/star_pause_music_button"
        android:layout_alignLeft="@+id/prev_music_button"
        android:layout_alignStart="@+id/prev_music_button" />
    <ListView
        android:id="@+id/music_list_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_above="@+id/the_music_Name">

    </ListView>

</RelativeLayout>
